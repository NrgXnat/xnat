plugins {
    id 'buildlogic.java-library-conventions'
}

group = 'org.nrg.dicom'

dependencies {
    api project(':framework')
    api project(':transaction')

    api libs.dcm4che5.dcm4che.core
    api libs.dcm4che5.dcm4che.imageio
    implementation libs.spring.orm
    implementation libs.spring.context
    implementation libs.commons.codec
    implementation libs.google.guava
    api libs.uuid.java.uuid.generator
    implementation libs.slf4j.api
    implementation libs.slf4j.log4j12

    compileOnly libs.google.jsr305

    testImplementation project(':test')
    testImplementation libs.ehcache.ehcache
    testImplementation libs.junit.junit
    testImplementation libs.spring.test
}

test {
    // LargeDicomObjectTest builds a DICOM larger than 2 GB to prove it can be anonymized, which
    // costs several GB of scratch and a minute or two. Opt in with -PlargeObjectTests.
    systemProperty 'test.largeObjects', project.hasProperty('largeObjectTests')
}
