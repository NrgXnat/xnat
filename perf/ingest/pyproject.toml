[project]
name = "xnat-ingest-perf"
version = "0.1.0"
description = "Wall-clock + NFS-write A/B performance harness for XNAT DICOM ingest across routes and anonymization modes."
requires-python = ">=3.11"
dependencies = [
    "pydicom>=3.0",
    "pynetdicom>=3.0",
    "numpy>=1.26",
]

[tool.uv]
package = false
